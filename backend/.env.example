## サーバー設定
# バックエンドサーバーが使用するポート番号 (通常は3001)
PORT=3001

# サーバーの実行環境 (開発時: development, 本番時: production)
NODE_ENV=development

## OpenRouter API 設定
# LLM (AIモデル) との通信に使用するOpenRouter APIキー
# ★ 必須：取得したキーを設定してください
# https://openrouter.ai/ で取得してください（アカウントをお持ちでない場合はアカウントの作成が必要です。またサービス利用のためのクレジットが必要です）
OPENROUTER_API_KEY=your_openrouter_api_key # ★ 取得したAPIキーに修正

## CORS 設定
# Frontendが実行されているURL。このURLからのアクセスのみ許可します。
# Frontendが http://localhost:5173 で実行されている場合はこのまま
CORS_ORIGIN=http://localhost:5173

## MCPサーバーパス
# 使用するMCPサーバーのビルド済み実行ファイルへのパスを設定します。
# 例：GitHub連携MCPサーバー (ビルド結果による)
MCP_SERVER_PATH=../mcp/dist/main.js
# ↑ 使用するMCPサーバーのパスに修正してください

## ログ設定
# 表示するログの最低レベル (info, warn, errorなど)
LOG_LEVEL=info

## GitHub App 設定
# GitHub連携に使用するGitHub Appの情報です。
# setting/Developer settings/GitHub Appsで生成できます。

# ★ 必須：取得したApp IDを設定してください
GITHUB_APP_ID=your_github_app_id

# setting/Developer settings/GitHub Apps/your_github_app_name/Permissions & events で、以下の権限を設定してください：
# - Contents: Read & write
# - Metadata: Read-only
# - Pull requests: Read & write
# (プロジェクトのREADMEやドキュメントで正確な権限リストを確認することを推奨します)

# GitHub Appの秘密鍵(.pem)ファイルへのパスを設定します。
# ★ 必須：ダウンロードした秘密鍵(.pem)ファイルをBackendディレクトリなどに置き、そのファイルへのパスを指定してください。
# setting/Developer settings/GitHub Apps/your_github_app_name/General の下の方にある"Private key"の"Download"ボタンを押すとダウンロードできます。
# 【重要】このファイルは安全に管理し、Gitリポジトリにはコミットしないでください。
GITHUB_APP_PRIVATE_KEY=./github-app-private-key.pem # ★ 配置した.pemファイルへのパスに修正

# GitHub Appを対象リポジトリにインストールした際に取得したInstallation ID
# ★ 必須：取得したInstallation IDを設定してください
# setting/application/your_github_app_name を開いたときに表示されるURLの末尾にある数字がInstallation IDです。
GITHUB_INSTALLATION_ID=your_github_installation_id

## 対象リポジトリ設定
# GitHub連携機能が操作する対象のリポジトリ情報です。GitHub Appをインストールしたリポジトリである必要があります。
# ★ 必須：対象リポジトリのオーナー名 (ユーザー名または組織名) を設定してください
GITHUB_TARGET_OWNER=your_github_owner_name

# ★ 必須：対象リポジトリの名前を設定してください
GITHUB_TARGET_REPO=your_github_repo_name

# プルリクエスト作成時のベースとなるブランチ名 (省略可能、デフォルトブランチが使われることが多い)
GITHUB_BASE_BRANCH=main

## その他オプション
# GitHub Enterprise Serverを使用する場合に設定 (通常はコメントアウト)
# GITHUB_API_BASE_URL=https://your.ghe.domain/api/v3